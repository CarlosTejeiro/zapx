# User-guide screenshots

Drop PNG screenshots (v0.20) here with these exact file names so the guide picks
them up:

| File | What to capture |
|---|---|
| `workspace.png` | Full window: sidebar (SESSIONS + MACROS), tab strip, a connected terminal card, bottom snippet bar. |
| `new-session.png` | New/Edit Session dialog with the auth selector and the "Use a saved credential (vault)" picker. |
| `vault-dialog.png` | Vault manager: entry list + the add/edit form (Name, Username, Password). |
| `macros-zone.png` | Sidebar MACROS section with folders/macros and the header icons (import ↓ / export ↑ / MobaXterm / new +). |
| `macro-editor.png` | Edit macro dialog: an `expect` step with the `.*` regex checkbox, a `send` step with the 🔒 vault picker, a `wait` step. |
| `sftp.png` | SFTP file browser with a transfer in progress. |
| `settings-terminal.png` | Settings → Terminal: theme selector, font family + size, cursor style. |

Keep them reasonably sized (≤ ~1600px wide). PNG preferred.
