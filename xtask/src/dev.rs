pub fn run(_args: &[String]) -> Result<(), Box<dyn std::error::Error>> {
    // TODO (Bloque 0 / T-12): spawn `pnpm dev` in frontend/ and `cargo tauri dev`
    // in crates/app/ concurrently, forwarding signals.
    todo!("cargo xtask dev — not implemented yet (Bloque 0 T-12)")
}
