pub fn run(_args: &[String]) -> Result<(), Box<dyn std::error::Error>> {
    // TODO (Bloque 0 / T-12): invoke `cargo tauri build` and collect the
    // platform-specific installer from the bundle output directory.
    todo!("cargo xtask package — not implemented yet (Bloque 0 T-12)")
}
