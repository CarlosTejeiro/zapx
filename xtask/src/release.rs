pub fn run(_args: &[String]) -> Result<(), Box<dyn std::error::Error>> {
    // TODO (Bloque 8): bump version across Cargo.toml + package.json,
    // commit, tag, push, trigger the release CI workflow.
    todo!("cargo xtask release — not implemented yet (Bloque 8)")
}
